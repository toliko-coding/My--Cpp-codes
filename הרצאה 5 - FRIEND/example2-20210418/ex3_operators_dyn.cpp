
#include "CArray.h"
/*
int main()
{
	CArray one(5), two(5);
	cout << " number of arrays instantiated : "<<CArray::getarrayCounter() << endl;
	if (one == two)
		cout << " Equal ";
	else
		cout << " Not Equal ";
	cout << endl;
	cin >> one;
	cout << one;
	cin >> two;
	cout << two;
	if (one == two)
		cout << " Equal ";
	else
		cout << " Not Equal ";
	cout << endl;
	CArray three(one);//CArray three=one;
	cout << " number of arrays instantiated : " << CArray::getarrayCounter() << endl;
	cout << one << ' ' << three << endl;
	cout << three[2] << endl;//rvalue  int var=three[2];
	three[2] = 100; //lvalue
	cout << three[2] << endl;//rvalue

	two = one; // two=two;
	cout << " number of arrays instantiated : " << CArray::getarrayCounter() << endl;
	cout << one << ' ' << two << endl;
}
*/

